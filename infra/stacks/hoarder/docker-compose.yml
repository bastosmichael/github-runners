version: '3.8'

services:
  hoarder:
    image: ghcr.io/hoarder-app/hoarder:latest
    container_name: hoarder
    restart: unless-stopped
    environment:
      - HOARDER_BASE_URL=http://localhost:3005
      - TZ=UTC
    ports:
      - "3005:3005"
    volumes:
      - /opt/hoarder/data:/data
